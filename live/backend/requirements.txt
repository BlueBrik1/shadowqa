# ShadowQA Live bridge — runtime dependencies.
# `shadowqa live start` installs these into live/backend/.venv on first run.
fastapi>=0.110,<1
uvicorn[standard]>=0.25
pydantic>=2.6
email-validator>=2.1
python-dotenv>=1.0
httpx>=0.27

# Optional: MongoDB instead of the default local JSON store (set MONGO_URL + DB_NAME).
motor>=3.3

# Optional: route Anthropic/OpenAI through an Emergent key instead of your own keys.
# pip install emergentintegrations --extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/

# Tests
pytest>=8
pytest-xdist>=3.5
