node_modules/
dist/
.env
.env.*
!.env.example
.shadowqa/
coverage/
*.vsix
*.pem
*.key
*.log
vscode-extension/out/
test-results/
dist-individual/
video/out/
video/src/generated/

# individual/extension/dist is deliberately NOT ignored: the built, load-unpacked-ready
# extension ships with the repository. Rebuild it with `npm run build:extension:individual`.
